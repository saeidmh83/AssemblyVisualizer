<Window x:Class="ILSpyVisualizer.AssemblyBrowser.AssemblyBrowserWindow"
        x:ClassModifier="internal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
        xmlns:AssemblyBrowser="clr-namespace:ILSpyVisualizer.AssemblyBrowser" 
        Title="{Binding Title}" 
        Height="300" 
        Width="300">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>

        <DataTemplate DataType="{x:Type AssemblyBrowser:MemberViewModel}">
            <Grid Margin="0, 2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <Image Source="{Binding Icon}"
                       Margin="0, 0, 6, 0"                    
                       Stretch="Uniform"
                       Width="16"
                       Height="16"                       
                       />
                <TextBlock Text="{Binding Text}"
                           Grid.Column="1" />
            </Grid>
        </DataTemplate>

        <DataTemplate DataType="{x:Type AssemblyBrowser:TypeViewModel}">
            <DataTemplate.Resources>
                <Style x:Key="TypeNameTextBlockStyle" TargetType="{x:Type TextBlock}">
                    <Setter Property="Text" Value="{Binding Name}"/>
                    <Setter Property="ToolTip" Value="{Binding FullName}"/>
                    <Setter Property="HorizontalAlignment" Value="Center"/>
                    <Setter Property="Margin" Value="8, 5, 8, 3"/>                    
                    <Setter Property="FontWeight" Value="Bold"/>
                </Style>
                <Style x:Key="BorderStyle" TargetType="{x:Type Border}">
                    <Setter Property="CornerRadius" Value="10"/>
                    <Setter Property="BorderThickness" Value="1"/>
                    <Setter Property="BorderBrush" Value="Black"/>
                    <Setter Property="Margin" Value="8"/>
                    <Setter Property="SnapsToDevicePixels" Value="True"/>
                </Style>
            </DataTemplate.Resources>

            <Grid HorizontalAlignment="Left"
                  Margin="5">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition />
                </Grid.RowDefinitions>
                <Grid HorizontalAlignment="Center">
                    <Border Style="{StaticResource BorderStyle}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition/>
                            </Grid.RowDefinitions>
                            <Border x:Name="brdDivider" BorderThickness="0, 0, 0, 1"
                                SnapsToDevicePixels="True"
                                BorderBrush="Black">
                                <TextBlock Style="{StaticResource TypeNameTextBlockStyle}"/>
                            </Border>
                            <ItemsControl x:Name="icMembers"
                                    ItemsSource="{Binding Members}"
                                      Grid.Row="1"
                                      Margin="5" />
                        </Grid>
                    </Border>
                    <Border Style="{StaticResource BorderStyle}">
                        <Border.Effect>
                            <DropShadowEffect/>
                        </Border.Effect>
                    </Border>
                </Grid>
                <ItemsControl ItemsSource="{Binding DerivedTypes}"
                              Grid.Row="1">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                </ItemsControl>
            </Grid>

            <DataTemplate.Triggers>
                <DataTrigger Binding="{Binding ShowMembers}" Value="False">
                    <Setter TargetName="icMembers" Property="Visibility" Value="Collapsed"/>
                    <Setter TargetName="brdDivider" Property="BorderThickness" Value="0"/>
                </DataTrigger>
            </DataTemplate.Triggers>
        </DataTemplate>
    </Window.Resources>
    <Grid UseLayoutRounding="True"
          TextOptions.TextFormattingMode="Display">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid>
            <StackPanel>
                <RadioButton GroupName="ViewMode" 
                             Content="Type Hierarchies" 
                             IsChecked="{Binding ShowTypeHierarchies}"
                             Margin="4"/>
                <RadioButton GroupName="ViewMode" 
                             Content="Single Types" 
                             IsChecked="{Binding ShowSingleTypes}"
                             Margin="4"/>
            </StackPanel>
        </Grid>

        <Grid Grid.Row="1">            
            <ScrollViewer HorizontalScrollBarVisibility="Auto"
                          VerticalScrollBarVisibility="Auto"
                          Visibility="{Binding ShowTypeHierarchies, Converter={StaticResource BooleanToVisibilityConverter}}">
                <ItemsControl ItemsSource="{Binding HierarchyRootTypes}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid>
                                <Border BorderThickness="0, 0, 0, 1"
                                        BorderBrush="Black">
                                    <ContentControl Content="{Binding}"
                                                    Margin="10, 15"/>
                                </Border>
                                <Border BorderThickness="0, 0, 0, 1"
                                        BorderBrush="Black">
                                    <Border.Effect>
                                        <DropShadowEffect/>
                                    </Border.Effect>
                                </Border>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Vertical"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                </ItemsControl>
            </ScrollViewer>
            <ScrollViewer VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled"
                          Visibility="{Binding ShowSingleTypes, Converter={StaticResource BooleanToVisibilityConverter}}">
                <ItemsControl ItemsSource="{Binding SingleTypes}"
                              Grid.Row="1">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                </ItemsControl>
            </ScrollViewer>
        </Grid>
    </Grid>
</Window>
